# Next.js Configuration
NODE_ENV=development
PORT=3000
HOSTNAME=localhost

# AI Code Analyst uses Claude Code CLI installed on the server
# Make sure 'claude' command is available in PATH

# GitHub API Token (optional but recommended to avoid rate limits)
# Create token at: https://github.com/settings/tokens
# Permissions needed: public_repo (read-only)
GITHUB_TOKEN=your_github_personal_access_token_here
